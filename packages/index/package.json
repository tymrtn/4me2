{
  "name": "@4me2/index",
  "version": "0.1.0",
  "description": "Discovery index with PostgreSQL + PostGIS for finding reviews across author nodes",
  "license": "Apache-2.0",
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "dev": "tsx watch src/index.ts",
    "test": "echo 'no tests yet'",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@4me2/schema": "workspace:*"
  },
  "devDependencies": {
    "tsx": "^4.19.0",
    "typescript": "^5.5.0",
    "vitest": "^2.0.0"
  }
}
